name: Checks
on: [push, pull_request]

jobs:
  test:
    name: DCOR-CKAN
    runs-on: ubuntu-latest
    container:
      image: ckan/ckan-dev:2.11.1
      options: --user root --hostname dcor-docker

    services:
      solr:
        image: ckan/ckan-solr:2.11-solr9
      postgres:
        image: raghavaalajangi95/dcor-postgresql:latest
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
          CKAN_DB_USER: ckandbuser
          CKAN_DB_PASSWORD: ckandbpassword
          CKAN_DB: ckandb
          DATASTORE_READONLY_USER: datastore_ro
          DATASTORE_READONLY_PASSWORD: datastore
          DATASTORE_DB: datastore
        options: --health-cmd "pg_isready" --health-interval 10s --health-timeout 5s --health-retries 10
      redis:
        image: redis:alpine
      minio:
        image: raghavaalajangi95/dcor-minio:latest
        env:
          MINIO_ROOT_USER: minioadmin
          MINIO_ROOT_PASSWORD: minioadmin
          MINIO_NEW_USER: dcor
          MINIO_NEW_SECRET_KEY: secretkey12345678
        options: --health-cmd "curl -f http://localhost:9000/minio/health/ready || exit 1" --health-interval 10s --health-timeout 5s --health-retries 10
        ports:
          - 9000:9000
          - 9001:9001

    env:
      CKAN_SQLALCHEMY_URL: postgresql://ckan_default:pass@postgres/ckan_test
      CKAN_DATASTORE_WRITE_URL: postgresql://datastore_write:pass@postgres/datastore_test
      CKAN_DATASTORE_READ_URL: postgresql://datastore_read:pass@postgres/datastore_test
      CKAN_SOLR_URL: http://solr:8983/solr/ckan
      CKAN_REDIS_URL: redis://redis:6379/1

    steps:
      - uses: actions/checkout@v4

      - name: Install DCOR extensions inside the DCOR-CKAN container
        run: |
          pip3 install dcor_control==0.12.6
          dcor inspect --assume-yes

      - name: Install extention & requirements
        run: |
          pip install .
          pip install -r ./ckanext/dc_view/tests/requirements.txt
          # Replace default path to CKAN core config file with the one on the container
          sed -i -e 's/use = config:.*/use = config:\/srv\/app\/src\/ckan\/test-core.ini/' docker-test.ini

      - name: Setup extension
        run: |
          ckan -c docker-test.ini db init

      - name: Wait for MinIO to be ready
        run: |
          until curl -f http://minio:9000/minio/health/ready; do sleep 2; done

      - name: Run tests
        run: |
          pytest -v --ckan-ini=test.ini \
             --cov=ckanext.dc_view \
             --cov-report=xml \
             --cov-append \
             --disable-warnings \
             ckanext/dc_view/tests/
